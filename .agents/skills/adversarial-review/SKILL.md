---
name: adversarial-review
description: 変更差分を批判的に点検したいときに使う。独立した視点で破壊的変更、運用リスク、説明不足、検証漏れを洗い出すレビュー専用 skill であり、対応可否の判断や実装修正は呼び出し元が担う。
---

# Adversarial Review

## いつ使うか

- 実装後に、self-review とは別視点の批判的レビューを追加したいとき
- 大きな変更や重要変更で、見落としを減らすために独立レビューを挟みたいとき
- review 対応や追加コミット前に、主張の弱い箇所や未確認事項を洗い出したいとき

## いつ使わないか

- まだ差分が固まっていないとき
- 呼び出し元がレビュー結果を評価したり実装へ戻したりできないとき
- 実装や修正までこの skill 単体にやらせたいとき

## 前後関係

- 実装 → [self-review](../self-review/SKILL.md) → 必要時に **adversarial-review** → 判断と修正は呼び出し元で実施
- [pr-followup](../pr-followup/SKILL.md) の修正案や反論方針を固める前に補助的に使ってよい

## 目的

独立した視点で差分を批判的に読み、破壊的変更、運用上の回帰、説明不足、検証漏れ、前提依存の弱さを洗い出す。
この skill はレビュー結果を返すところまでを責務とし、対応可否の判断、実装修正、再検証、コミットは呼び出し元に残す。

## 正本

- repo 共通ルールは `AGENTS.md` を正本とする

## 対象

- 作業ブランチと base branch の差分
- 直近コミット単位の差分
- 実装意図に対する説明の整合
- 実施済み / 未実施の検証
- 残リスクとして扱うには重い未確認事項

## 判断ルール

- 自分で実装や修正に進まず、まず懸念の列挙と根拠提示に徹する
- 懸念は重大度順に整理し、差分や実行結果に根拠があるものを優先する
- 仕様未確認や検証不足は、その不足自体をリスクとして扱う
- 「たぶん大丈夫」で閉じず、どの条件なら危ないかを具体化する
- 根拠が弱い懸念は、断定せず open question として分ける
- サブエージェントが使える環境では、呼び出し元と独立した文脈のサブエージェントにレビューだけを依頼してよい
- サブエージェントにはコード修正、コミット、push をさせず、findings の報告に専念させる

## 推奨手順

1. 現在 branch とレビュー対象の差分範囲を確認する
2. サブエージェントが使える環境なら、レビュー専用の独立サブエージェントに差分確認を依頼する
3. `main` または作業元 branch、必要なら直近コミットとの差分を読む
4. 変更ファイルごとに、主題から外れた変更や説明不足を洗う
5. 挙動破壊、境界条件、データ整合性、運用回帰の順で懸念を探す
6. 検証コマンドと実行結果を確認し、未検証箇所を洗い出す
7. 懸念を `要対応 / open question / 残リスク` に分けて整理する
8. 呼び出し元が判断しやすいよう、各懸念に根拠と影響を書く

## 停止条件

- 差分の主題が複数に割れていて、レビュー観点を定められない
- 差分や検証結果が不足し、根拠を持って懸念を述べられない
- 修正指示や実装まで求められており、レビュー専用の責務を保てない

## 出力

- 重大度順の findings
- 各 finding の根拠
- open question
- 残リスク
- 呼び出し元側で判断・実装すべき事項
